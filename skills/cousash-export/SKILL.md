---
name: cousash-export
description: Export this device's Cousash Codex usage snapshot JSON for manual import on another device.
---

Run the bundled snapshot exporter:

```bash
python3 ../codex-usage-dashboard/scripts/export_snapshot.py
```

The command prints the generated `cousash-<device-short-code>.json` path.
