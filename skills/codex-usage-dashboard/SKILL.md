---
name: codex-usage-dashboard
description: Open a local read-only Codex usage dashboard. Use when the user asks to view Codex usage, token usage, per-conversation usage, output tokens, cache hit rate, or cost estimates from Codex local sessions.
---

# Codex Usage Dashboard

Run:

```bash
python scripts/open_dashboard.py
```

Use `python3` on macOS if needed.
